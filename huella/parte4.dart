import 'package:flutter/material.dart';

class parte4 extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Diseño de Apps', style: TextStyle(fontSize: 35.0),)
    );
  }
}