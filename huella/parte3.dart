import 'package:flutter/material.dart';

class parte3 extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Center(
        child: Text('TI-41', style: TextStyle(fontSize: 35.0),)
    );
  }
}